'use client';
import {useMemo,useState} from 'react';
import { DropdownControl } from './ui';
type Tool='gpa'|'grade'|'citation'|'schedule'|'quiz'|'flashcards'|'word-count'|'reading-level'|'typing'|'vocabulary'|'essay'|'countdown'|'marks-needed'|'pass-marks'|'final-grade'|'study-hours'|'question-paper';
const tools:{id:Tool;name:string;group:string;help:string}[]=[{id:'gpa',name:'GPA Calculator by Country',group:'Grades',help:'Calculate weighted GPA using common 4.0, 5.0, 10.0 or percentage scales.'},{id:'grade',name:'Grade Percentage Calculator',group:'Grades',help:'Convert earned marks into percentage and grade.'},{id:'marks-needed',name:'Marks Needed Calculator',group:'Grades',help:'Find marks required from remaining assessment points.'},{id:'pass-marks',name:'What Marks Do I Need to Pass?',group:'Grades',help:'Calculate the exact remaining pass requirement.'},{id:'final-grade',name:'Final Exam Grade Needed',group:'Grades',help:'Find the final-exam percentage required for a target course grade.'},{id:'citation',name:'Citation Generator',group:'Write',help:'Format a basic book, article or web citation in APA, MLA or Chicago.'},{id:'word-count',name:'Word Counter',group:'Write',help:'Count words, characters, sentences and paragraphs.'},{id:'reading-level',name:'Reading Level Checker',group:'Write',help:'Estimate readability using sentence and syllable signals.'},{id:'vocabulary',name:'Vocabulary Difficulty Checker',group:'Write',help:'Flag long and uncommon-looking words for review.'},{id:'essay',name:'Essay Outline Generator',group:'Write',help:'Create a structured thesis, body and conclusion outline.'},{id:'schedule',name:'Study Schedule Generator',group:'Study',help:'Distribute subjects across available days and sessions.'},{id:'study-hours',name:'Study Hours Planner',group:'Study',help:'Calculate a confidence-adjusted daily study target.'},{id:'countdown',name:'Exam Countdown Planner',group:'Study',help:'Create revision phases leading to an exam.'},{id:'flashcards',name:'Flashcard Generator',group:'Practice',help:'Turn term-definition notes into printable study cards.'},{id:'quiz',name:'Quiz Generator',group:'Practice',help:'Generate short-answer and multiple-choice prompts from notes.'},{id:'question-paper',name:'Question Paper Maker',group:'Practice',help:'Build a balanced question paper with marks and instructions.'},{id:'typing',name:'Typing Speed Test',group:'Practice',help:'Measure words per minute and accuracy from a timed sample.'}];
const defaults:Record<Tool,[string,string]>= {gpa:['Mathematics,85,3\nEnglish,78,2\nScience,92,4','4.0 scale'],grade:['78','100'],citation:['Practical E-commerce\nAsif Khan Nirob\n2026\nContra Press\nhttps://example.com/book','APA'],schedule:['Mathematics: 8\nEnglish: 4\nScience: 10','14 days, 3 hours/day'],quiz:['Photosynthesis: Plants convert light energy into chemical energy.\nMitochondria: The organelle responsible for cellular respiration.','6'],flashcards:['Revenue: Total income before expenses.\nProfit: Revenue remaining after all costs.\nMargin: Profit expressed as a percentage of revenue.',''], 'word-count':['Paste or write your text here. This tool counts words, characters, sentences and paragraphs.',''], 'reading-level':['Clear writing uses familiar words and varied sentence lengths. It explains difficult ideas without unnecessary complexity.',''],typing:['Clear communication helps teams make better decisions and complete meaningful work.','60\nClear communication helps teams make better decisions and complete meaningful work.'],vocabulary:['Leverage comprehensive methodologies to facilitate organizational interoperability and practical customer outcomes.',''],essay:['How digital tools improve small-business decision making','Digital tools improve decisions by making costs visible, reducing manual work, and supporting faster experiments.'],countdown:['2026-12-10','Mathematics, English, Science'],'marks-needed':['52','100\n40\n60'],'pass-marks':['38','50\n100'], 'final-grade':['72','40\n80'], 'study-hours':['80','30\n50'], 'question-paper':['E-commerce fundamentals\nProfit, revenue, contribution margin, customer acquisition cost, returns','100\n120']};
const c='w-full rounded-xl border border-gray-300 bg-white px-3 py-2.5 text-sm outline-none focus:border-gray-900 focus:ring-2 focus:ring-gray-100';const btn='rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white';const num=(x:string)=>Number(x)||0;
export function EducationToolsStudio(){const[active,setActive]=useState<Tool>('gpa');const[a,setA]=useState(defaults.gpa[0]);const[b,setB]=useState(defaults.gpa[1]);const groups=Array.from(new Set(tools.map(x=>x.group)));const selected=tools.find(x=>x.id===active)!;const choose=(id:Tool)=>{setActive(id);setA(defaults[id][0]);setB(defaults[id][1])};const result=useMemo(()=>run(active,a,b),[active,a,b]);return <div className="grid gap-5 xl:grid-cols-[270px_minmax(0,1fr)]"><aside className="self-start rounded-2xl border bg-white p-3 shadow-sm xl:sticky xl:top-4"><div className="px-3 pb-3 pt-2"><p className="text-xs font-bold uppercase tracking-[.18em] text-sky-600">17 learning utilities</p><h2 className="mt-1 text-lg font-bold">Education Tools</h2></div>{groups.map(g=><div key={g} className="mb-3"><p className="px-3 py-1 text-[10px] font-bold uppercase tracking-[.16em] text-gray-400">{g}</p>{tools.filter(x=>x.group===g).map(x=><button key={x.id} onClick={()=>choose(x.id)} className={`mb-1 w-full rounded-xl px-3 py-2 text-left text-sm ${active===x.id?'bg-gray-950 font-semibold text-white':'text-gray-700 hover:bg-gray-100'}`}>{x.name}</button>)}</div>)}</aside><section className="min-w-0 space-y-5"><header className="rounded-2xl border bg-gradient-to-r from-sky-50 to-blue-50 p-5"><p className="text-xs font-bold uppercase tracking-[.16em] text-sky-700">Education Tools</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-1 text-sm text-gray-600">{selected.help}</p></header><div className="grid gap-5 lg:grid-cols-2"><section className="space-y-4 rounded-2xl border bg-white p-5 shadow-sm"><label className="block"><span className="mb-1 block text-sm font-medium">{labels[active][0]}</span><textarea className={c} rows={active==='word-count'||active==='reading-level'||active==='vocabulary'?14:9} value={a} onChange={e=>setA(e.target.value)}/></label>{labels[active][1]&&<label className="block"><span className="mb-1 block text-sm font-medium">{labels[active][1]}</span>{['gpa','citation'].includes(active)?<DropdownControl className={c} ariaLabel={labels[active][1]} value={b} onChange={setB} options={active==='gpa'?['4.0 scale','5.0 scale','10.0 scale','Percentage']:['APA','MLA','Chicago']}/>:<textarea className={c} rows={4} value={b} onChange={e=>setB(e.target.value)}/>}</label>}<p className="text-xs leading-5 text-gray-500">Verify institutional grading rules, citation requirements and exam policies. Generated study material should be reviewed by the learner or teacher.</p></section><section className="space-y-4 rounded-2xl border bg-white p-5 shadow-sm"><p className="text-sm font-semibold">Result</p><pre className="max-h-[700px] min-h-64 overflow-auto whitespace-pre-wrap rounded-xl bg-gray-950 p-5 text-sm leading-7 text-emerald-300">{result}</pre><button className={btn} onClick={()=>navigator.clipboard.writeText(result)}>Copy result</button></section></div></section></div>}
const labels:Record<Tool,[string,string]>={gpa:['Courses: name, percentage, credits','Scale'],grade:['Marks earned','Total marks'],citation:['Title, author, year, publisher, URL','Citation style'],schedule:['Subjects and estimated hours','Days and available hours/day'],quiz:['Term: definition — one per line','Number of questions'],flashcards:['Term: definition — one per line',''],'word-count':['Text',''],'reading-level':['Text',''],typing:['Reference text','Elapsed seconds, then typed text'],vocabulary:['Text',''],essay:['Essay topic','Thesis or central claim'],countdown:['Exam date','Subjects — comma-separated'],'marks-needed':['Marks already earned','Total course points, completed points, target %'],'pass-marks':['Marks already earned','Completed points, total course points'],'final-grade':['Current course percentage','Final exam weight %, target course %'],'study-hours':['Estimated syllabus hours','Days remaining, confidence %'],'question-paper':['Subject, then syllabus topics','Total marks, duration in minutes']};
function pctGrade(p:number){return p>=90?'A+':p>=80?'A':p>=70?'B':p>=60?'C':p>=50?'D':'F'}function words(v:string){return v.trim().match(/[\p{L}\p{N}'’-]+/gu)||[]}function syllables(word:string){const w=word.toLowerCase().replace(/[^a-z]/g,'');if(!w)return 1;return Math.max(1,(w.replace(/e$/,'').match(/[aeiouy]+/g)||[]).length)}
function run(t:Tool,a:string,b:string){if(t==='gpa'){const max=b.startsWith('4')?4:b.startsWith('5')?5:b.startsWith('10')?10:100;const rows=a.split('\n').map(x=>x.split(',')).filter(x=>x.length>=3);let points=0,credits=0;const lines=rows.map(([name,mark,credit])=>{const p=num(mark),c=num(credit),gp=max===100?p:max===4?(p>=93?4:p>=90?3.7:p>=87?3.3:p>=83?3:p>=80?2.7:p>=77?2.3:p>=73?2:p>=70?1.7:p>=67?1.3:p>=60?1:0):p/100*max;points+=gp*c;credits+=c;return `${name.trim()}: ${gp.toFixed(2)} × ${c} credits`});return `${b} GPA: ${credits?(points/credits).toFixed(2):'0.00'}\nTotal credits: ${credits}\n\n${lines.join('\n')}\n\nScale mappings vary by institution and country; confirm the official policy.`}if(t==='grade'){const p=num(b)?num(a)/num(b)*100:0;return `Percentage: ${p.toFixed(2)}%\nIndicative grade: ${pctGrade(p)}\nMarks: ${a} / ${b}\nMarks not earned: ${Math.max(0,num(b)-num(a))}`}
if(t==='marks-needed'){const [total,completed,target]=b.split('\n').map(num),remaining=total-completed,needed=total*target/100-num(a);return `Target: ${target}% (${(total*target/100).toFixed(2)} points)\nRemaining available: ${remaining} points\nNeeded from remaining work: ${Math.max(0,needed).toFixed(2)} points\nRequired remaining percentage: ${remaining?Math.max(0,needed)/remaining*100:0}%\nStatus: ${needed>remaining?'Target is no longer mathematically reachable.':needed<=0?'Target already secured.':'Target remains reachable.'}`}
if(t==='pass-marks'){const [completed,total]=b.split('\n').map(num),target=total*.5,needed=target-num(a),remaining=total-completed;return `Assumed pass mark: 50% (${target} points)\nMarks still needed: ${Math.max(0,needed).toFixed(2)}\nRequired on remaining points: ${remaining?Math.max(0,needed)/remaining*100:0}%\n${needed<=0?'You have already reached the assumed pass threshold.':needed>remaining?'Passing is not mathematically possible under these inputs.':'Passing remains achievable.'}`}
if(t==='final-grade'){const [weight,target]=b.split('\n').map(num),current=num(a),required=(target-current*(1-weight/100))/(weight/100);return `Current grade: ${current}%\nFinal exam weight: ${weight}%\nTarget course grade: ${target}%\nRequired final exam grade: ${required.toFixed(2)}%\n${required>100?'Target is not reachable from the final exam alone.':required<=0?'Target is already secured.':'Target is mathematically reachable.'}`}
if(t==='citation'){const [title='',author='',year='',publisher='',url='']=a.split('\n');if(b==='MLA')return `${author}. “${title}.” ${publisher}, ${year}. ${url}.`;if(b==='Chicago')return `${author}. “${title}.” ${publisher}, ${year}. ${url}.`;return `${author} (${year}). ${title}. ${publisher}. ${url}`}
if(t==='word-count'){const w=words(a),sent=(a.match(/[.!?]+(?:\s|$)/g)||[]).length,paras=a.split(/\n\s*\n/).filter(x=>x.trim()).length;return `Words: ${w.length.toLocaleString()}\nCharacters: ${a.length.toLocaleString()}\nCharacters without spaces: ${a.replace(/\s/g,'').length.toLocaleString()}\nSentences: ${sent}\nParagraphs: ${paras}\nEstimated reading time: ${Math.max(1,Math.ceil(w.length/200))} minutes`}
if(t==='reading-level'){const w=words(a),sent=Math.max(1,(a.match(/[.!?]+(?:\s|$)/g)||[]).length),syll=w.reduce((s,x)=>s+syllables(x),0),score=206.835-1.015*(w.length/sent)-84.6*(syll/Math.max(1,w.length)),grade=0.39*(w.length/sent)+11.8*(syll/Math.max(1,w.length))-15.59;return `Flesch reading ease: ${score.toFixed(1)} / 100\nApproximate US grade level: ${Math.max(0,grade).toFixed(1)}\nWords per sentence: ${(w.length/sent).toFixed(1)}\nSyllables per word: ${(syll/Math.max(1,w.length)).toFixed(2)}\n\nThis English-language estimate is a writing signal, not a measure of reader intelligence.`}
if(t==='vocabulary'){const w=words(a),unique=Array.from(new Set(w.map(x=>x.toLowerCase()))),hard=unique.filter(x=>x.length>=10||syllables(x)>=4).sort((x,y)=>y.length-x.length);return `Words: ${w.length}\nUnique words: ${unique.length}\nPotentially difficult words: ${hard.length}\n\nREVIEW LIST\n${hard.map(x=>`• ${x} (${syllables(x)} syllables)`).join('\n')||'No obviously difficult words detected.'}\n\nKeep technical terms when they are precise; explain them at first use.`}
if(t==='essay')return `TITLE\n${a}\n\nTHESIS\n${b}\n\nI. INTRODUCTION\n• Context and importance\n• Define the central problem\n• Present the thesis\n\nII. BODY 1 — First reason\n• Claim\n• Evidence or example\n• Link to thesis\n\nIII. BODY 2 — Second reason\n• Claim\n• Evidence or example\n• Address a limitation\n\nIV. BODY 3 — Implications\n• Practical effect\n• Counterargument and response\n\nV. CONCLUSION\n• Synthesize the argument\n• Restate significance\n• End with a forward-looking insight`;
if(t==='schedule'){const subjects=a.split('\n').map(x=>{const[n,h]=x.split(':');return{name:n.trim(),hours:num(h)}}).filter(x=>x.name),[days,hpd]=b.match(/[\d.]+/g)?.map(Number)||[7,2],total=subjects.reduce((s,x)=>s+x.hours,0),sessions:string[]=[];for(let d=1;d<=days;d++){const sub=subjects[(d-1)%subjects.length];sessions.push(`Day ${d}: ${sub.name} — ${Math.min(hpd,sub.hours).toFixed(1)}h · ${d%7===0?'weekly review':'practice + recall'}`)}return `Workload: ${total} hours · Capacity: ${days*hpd} hours\nStatus: ${days*hpd>=total?'Capacity covers the estimate.':'Add days/hours or reduce scope.'}\n\n${sessions.join('\n')}`}
if(t==='study-hours'){const [days,confidence]=b.split('\n').map(num),daily=num(a)/Math.max(1,days)*(1+(100-confidence)/100);return `Recommended daily study: ${daily.toFixed(1)} hours\nWeekly target: ${(daily*7).toFixed(1)} hours\n30-minute sessions: ${Math.ceil(daily*2)} per day\nReserve the final 20% of available days for practice and revision.`}
if(t==='countdown'){const exam=new Date(a),today=new Date(),left=Math.max(0,Math.ceil((+exam-+today)/86400000)),subs=b.split(',').map(x=>x.trim());return `Exam: ${exam.toLocaleDateString(undefined,{dateStyle:'full'})}\nDays remaining: ${left}\n\nPHASE 1 — Learn and organize (${Math.round(left*.5)} days)\n${subs.map(x=>`• ${x}: concepts + notes`).join('\n')}\n\nPHASE 2 — Practice (${Math.round(left*.3)} days)\n• Timed problems and past questions\n\nPHASE 3 — Final revision (${Math.max(1,Math.round(left*.2))} days)\n• Error log, recall and sleep protection`}
if(t==='flashcards'){return a.split('\n').map((x,i)=>{const[q,...rest]=x.split(':');return `CARD ${i+1}\nFRONT: ${q.trim()}\nBACK: ${rest.join(':').trim()}`}).join('\n\n')}
if(t==='quiz'){const pairs=a.split('\n').map(x=>x.split(':')).filter(x=>x.length>1),count=Math.min(num(b)||pairs.length,pairs.length*2);const qs:string[]=[];for(let i=0;i<count;i++){const p=pairs[i%pairs.length],term=p[0].trim(),def=p.slice(1).join(':').trim();qs.push(i%2===0?`${i+1}. Define ${term}.`:`${i+1}. Which term matches this description? “${def}”`)}return `QUIZ\n${qs.join('\n')}\n\nANSWER KEY\n${qs.map((_,i)=>`${i+1}. ${pairs[i%pairs.length][i%2===0?1:0].trim()}`).join('\n')}`}
if(t==='question-paper'){const [subject,...topics]=a.split('\n'),[marks,duration]=b.split('\n').map(num),topicList=topics.join(',').split(',').map(x=>x.trim()).filter(Boolean);const short=Math.round(marks*.3),long=marks-short;return `${subject.toUpperCase()}\nTime: ${duration} minutes · Total marks: ${marks}\n\nInstructions: Answer all questions. Show reasoning where applicable.\n\nSECTION A — Short answer (${short} marks)\n${topicList.map((x,i)=>`${i+1}. Explain ${x} with one practical example. [${Math.max(2,Math.floor(short/topicList.length))}]`).join('\n')}\n\nSECTION B — Applied questions (${long} marks)\n1. Compare and connect two major ideas from the syllabus. [${Math.round(long/2)}]\n2. Analyze a realistic scenario using the course concepts. [${long-Math.round(long/2)}]\n\nTeacher review required before use.`}
const [secondsLine,...typedLines]=b.split('\n'),typed=typedLines.join('\n'),seconds=Math.max(1,num(secondsLine)),refWords=words(a),typedWords=words(typed);let correct=0;typedWords.forEach((x,i)=>{if(x===refWords[i])correct++});return `Gross speed: ${(typedWords.length/(seconds/60)).toFixed(1)} WPM\nAccuracy: ${typedWords.length?correct/typedWords.length*100:0}%\nNet speed: ${(correct/(seconds/60)).toFixed(1)} WPM\nTyped words: ${typedWords.length}\nElapsed: ${seconds} seconds`}
export const calculateEducationTool = (tool:string,a:string,b:string) => run(tool as Tool,a,b);
