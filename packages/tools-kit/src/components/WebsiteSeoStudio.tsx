'use client';
import { useMemo,useState } from 'react';
import { DropdownControl } from './ui';

type SeoTool='title'|'serp'|'og'|'robots'|'sitemap'|'canonical'|'broken'|'density'|'slug'|'redirect-map'|'domain'|'technology'|'page-size'|'vitals'|'facebook-browser'|'share-debug'|'multilingual-slug'|'product-schema'|'launch';
const tools:{id:SeoTool;name:string;group:string;help:string}[]=[
{id:'title',name:'Meta Title Length Checker',group:'Metadata',help:'Check title length, pixel estimate and search-result readability.'},{id:'serp',name:'SERP Preview Tool',group:'Metadata',help:'Preview desktop and mobile-style organic search snippets.'},{id:'og',name:'Open Graph Preview',group:'Metadata',help:'Preview link cards and generate Open Graph tags.'},{id:'share-debug',name:'Social Sharing Preview Debugger',group:'Metadata',help:'Inspect live Open Graph and Twitter metadata.'},
{id:'robots',name:'Robots.txt Generator',group:'Crawl & Index',help:'Create a crawl policy with sitemap and AI crawler controls.'},{id:'sitemap',name:'Sitemap Generator',group:'Crawl & Index',help:'Build standards-compliant sitemap XML from a URL list.'},{id:'canonical',name:'Canonical URL Checker',group:'Crawl & Index',help:'Compare a live page canonical with its final URL.'},{id:'broken',name:'Broken Link Checker',group:'Crawl & Index',help:'Check a sample of links discovered on a public page.'},{id:'redirect-map',name:'Redirect Mapping Generator',group:'Crawl & Index',help:'Convert old/new URL pairs into CSV, Apache or Nginx rules.'},
{id:'density',name:'Keyword Density Checker',group:'Content',help:'Count words and phrases without encouraging keyword stuffing.'},{id:'slug',name:'Slug Generator',group:'Content',help:'Create clean, lowercase URL slugs.'},{id:'multilingual-slug',name:'Multilingual Slug Generator',group:'Content',help:'Preserve Unicode or transliterate common Bangla characters.'},{id:'product-schema',name:'Product Schema Generator',group:'Content',help:'Generate validated Product and Offer JSON-LD.'},
{id:'domain',name:'Domain Name Generator',group:'Research',help:'Generate memorable domain candidates from a topic and benefit.'},{id:'technology',name:'Website Technology Checker',group:'Research',help:'Detect common technologies from public page signals.'},{id:'page-size',name:'Page Size Calculator',group:'Research',help:'Measure the downloaded HTML and declared resource counts.'},
{id:'vitals',name:'Core Web Vitals Checklist',group:'Quality',help:'Create a practical LCP, INP and CLS improvement checklist.'},{id:'facebook-browser',name:'Facebook In-App Browser Tester',group:'Quality',help:'Audit mobile, redirect and sharing risks for Facebook traffic.'},{id:'launch',name:'Website Launch Checklist Generator',group:'Quality',help:'Track essential launch checks and copy a handoff report.'},];
const c='w-full rounded-xl border border-gray-300 bg-white px-3 py-2.5 text-sm outline-none focus:border-gray-900 focus:ring-2 focus:ring-gray-100';const btn='rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-gray-800 disabled:opacity-40';
const defaults:Record<SeoTool,[string,string]>= {title:['Free Business Tools for Better Decisions',''],serp:['Free Business Tools for Better Decisions','Practical calculators and generators for commerce, creators and everyday work.\nhttps://example.com/free-tools'],og:['Create Better Business Decisions','Free private tools for practical everyday work.\nhttps://example.com/social-card.jpg'],robots:['https://example.com/sitemap.xml','Allow'],sitemap:['https://example.com/\nhttps://example.com/products\nhttps://example.com/contact','weekly'],canonical:['https://example.com/page',''],broken:['https://example.com',''],density:['ecommerce','Ecommerce businesses need clear ecommerce metrics. Revenue alone does not show ecommerce profit after delivery, returns and advertising.'],slug:['A Better Product Page: 10 Practical Ideas',''], 'redirect-map':['/old-page,/new-page\n/previous-product,/products/new-product','CSV'],domain:['commerce tools','better decisions'],technology:['https://example.com',''],'page-size':['https://example.com',''],vitals:['E-commerce store','Needs improvement'], 'facebook-browser':['https://example.com',''], 'share-debug':['https://example.com',''],'multilingual-slug':['বাংলাদেশের সেরা অনলাইন শপ','Preserve Unicode'],'product-schema':['Wireless Earbuds','2490,BDT,InStock\nClear sound and long battery life.'],launch:['Business website','Pre-launch']};
const n=(v:string)=>Number(v)||0;const esc=(v:string)=>v.replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]!));
const slugify=(v:string,preserve=true)=>v.normalize('NFKC').toLowerCase().trim().replace(preserve?/[^\p{L}\p{N}]+/gu:/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
const banglaMap:Record<string,string>={'আ':'a','ই':'i','ঈ':'i','উ':'u','এ':'e','ও':'o','ক':'k','খ':'kh','গ':'g','ঘ':'gh','চ':'ch','ছ':'chh','জ':'j','ঝ':'jh','ট':'t','ঠ':'th','ড':'d','ঢ':'dh','ত':'t','থ':'th','দ':'d','ধ':'dh','ন':'n','প':'p','ফ':'f','ব':'b','ভ':'bh','ম':'m','য':'y','র':'r','ল':'l','শ':'sh','ষ':'sh','স':'s','হ':'h','ং':'ng','য়':'y','া':'a','ি':'i','ী':'i','ু':'u','ূ':'u','ে':'e','ৈ':'oi','ো':'o','ৌ':'ou','্':''};

export function WebsiteSeoStudio({endpoint='/api/url-inspect'}:{endpoint?:string}){const [active,setActive]=useState<SeoTool>('title');const [a,setA]=useState(defaults.title[0]);const [b,setB]=useState(defaults.title[1]);const [remote,setRemote]=useState('');const [busy,setBusy]=useState(false);const [copied,setCopied]=useState(false);const selected=tools.find(t=>t.id===active)!;const groups=Array.from(new Set(tools.map(t=>t.group)));const choose=(id:SeoTool)=>{setActive(id);setA(defaults[id][0]);setB(defaults[id][1]);setRemote('')};const local=useMemo(()=>run(active,a,b),[active,a,b]);const result=remote||local;const isRemote=['canonical','broken','technology','page-size','facebook-browser','share-debug'].includes(active);const inspect=async()=>{setBusy(true);setRemote('');try{const res=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url:a,mode:active==='broken'?'broken-links':'seo-audit'})});setRemote(JSON.stringify(await res.json(),null,2))}catch(e){setRemote(e instanceof Error?e.message:'Inspection failed')}finally{setBusy(false)}};return <div className="grid gap-5 xl:grid-cols-[270px_minmax(0,1fr)]"><aside className="self-start rounded-2xl border border-gray-200 bg-white p-3 shadow-sm xl:sticky xl:top-4"><div className="px-3 pb-3 pt-2"><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-600">19 focused utilities</p><h2 className="mt-1 text-lg font-bold">Website &amp; SEO Tools</h2></div>{groups.map(g=><div key={g} className="mb-3"><p className="px-3 py-1 text-[10px] font-bold uppercase tracking-[.16em] text-gray-400">{g}</p>{tools.filter(t=>t.group===g).map(t=><button key={t.id} onClick={()=>choose(t.id)} className={`mb-1 w-full rounded-xl px-3 py-2 text-left text-sm ${active===t.id?'bg-gray-950 font-semibold text-white':'text-gray-700 hover:bg-gray-100'}`}>{t.name}</button>)}</div>)}</aside><section className="min-w-0 space-y-5"><header className="rounded-2xl border border-gray-200 bg-gradient-to-r from-emerald-50 to-cyan-50 p-5"><p className="text-xs font-bold uppercase tracking-[.16em] text-emerald-700">Website &amp; SEO Tools</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-1 text-sm text-gray-600">{selected.help}</p></header><div className="grid gap-5 lg:grid-cols-2"><section className="space-y-4 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><Inputs tool={active} a={a} b={b} setA={setA} setB={setB}/>{isRemote&&<button className={btn} disabled={busy} onClick={inspect}>{busy?'Inspecting…':'Inspect public page'}</button>}<p className="text-xs leading-5 text-gray-500">Generators run locally. Public page checks use a guarded request that blocks private and local network destinations.</p></section><section className="space-y-4 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><Preview tool={active} a={a} b={b}/><div className="flex items-center justify-between"><p className="text-sm font-semibold">Result</p><span className="text-xs text-gray-400">{result.length.toLocaleString()} characters</span></div><pre className="max-h-[620px] min-h-52 overflow-auto whitespace-pre-wrap break-words rounded-xl bg-gray-950 p-4 text-xs leading-5 text-emerald-300">{result||'Complete the inputs or run the page inspection.'}</pre><button className={btn} disabled={!result} onClick={async()=>{await navigator.clipboard.writeText(result);setCopied(true);setTimeout(()=>setCopied(false),1200)}}>{copied?'Copied':'Copy result'}</button></section></div></section></div>}

function Inputs({tool,a,b,setA,setB}:{tool:SeoTool;a:string;b:string;setA:(v:string)=>void;setB:(v:string)=>void}){const url=['canonical','broken','technology','page-size','facebook-browser','share-debug'].includes(tool);const labels:Partial<Record<SeoTool,[string,string]>>={serp:['Meta title','Description, then URL on a new line'],og:['Card title','Description, then image URL'],robots:['Sitemap URL','Crawler policy'],sitemap:['URLs — one per line','Change frequency'],density:['Target keyword or phrase','Page text'], 'redirect-map':['Old URL, new URL — one pair per line','Output format'],domain:['Topic or keyword','Benefit or differentiator'],vitals:['Website type','Current assessment'],'multilingual-slug':['Title or phrase','Slug mode'],'product-schema':['Product name','Price, currency, availability — then description'],launch:['Website type','Launch stage']};const pair=labels[tool];return <>{pair&&['robots','sitemap','redirect-map','vitals','multilingual-slug','launch'].includes(tool)?<label className="block"><span className="mb-1 block text-sm font-medium">{pair[1]}</span><DropdownControl className={c} ariaLabel={pair[1]} value={b} onChange={setB} options={({robots:['Allow','Block admin','Block all','Block AI training crawlers'],sitemap:['daily','weekly','monthly','yearly'],'redirect-map':['CSV','Apache .htaccess','Nginx'],vitals:['Good','Needs improvement','Poor'],'multilingual-slug':['Preserve Unicode','Transliterate Bangla','ASCII only'],launch:['Planning','Pre-launch','Post-launch']} as Partial<Record<SeoTool,string[]>>)[tool]!}/></label>:null}<label className="block"><span className="mb-1 block text-sm font-medium">{url?'Public page URL':pair?.[0]||'Input'}</span>{url||tool==='title'||tool==='slug'?<input className={c} value={a} onChange={e=>setA(e.target.value)}/>:<textarea className={c} rows={tool==='density'||tool==='sitemap'||tool==='redirect-map'?11:6} value={a} onChange={e=>setA(e.target.value)}/>}</label>{pair&&!['robots','sitemap','redirect-map','vitals','multilingual-slug','launch'].includes(tool)&&<label className="block"><span className="mb-1 block text-sm font-medium">{pair[1]}</span><textarea className={c} rows={tool==='density'?12:5} value={b} onChange={e=>setB(e.target.value)}/></label>}{['robots','sitemap','redirect-map','vitals','multilingual-slug','launch'].includes(tool)&&tool!=='title'&&<p className="text-xs text-gray-500">Selected option: <b>{b}</b></p>}</>}

function Preview({tool,a,b}:{tool:SeoTool;a:string;b:string}){if(tool==='serp'){const [desc,url='https://example.com/page']=b.split('\n');return <div className="rounded-xl border p-5 font-sans"><p className="text-sm text-emerald-700">{url}</p><p className="mt-1 text-xl text-blue-800">{a||'Page title'}</p><p className="mt-1 text-sm leading-5 text-gray-600">{desc}</p></div>}if(tool==='og'){const [desc,image]=b.split('\n');return <div className="overflow-hidden rounded-xl border"><div className="aspect-[1.91/1] bg-gray-100">{image?<img src={image} alt="Open Graph preview" className="h-full w-full object-cover"/>:null}</div><div className="p-4"><p className="font-semibold">{a}</p><p className="text-sm text-gray-600">{desc}</p></div></div>}if(tool==='title')return <div className="rounded-xl border p-4"><p className="text-xl text-blue-800">{a}</p><p className={`mt-2 text-sm font-semibold ${a.length>=30&&a.length<=60?'text-emerald-700':'text-amber-700'}`}>{a.length} characters · approximately {Math.round(a.length*8.2)} px</p></div>;return null}

function run(tool:SeoTool,a:string,b:string){if(['canonical','broken','technology','page-size','facebook-browser','share-debug'].includes(tool))return '';if(tool==='title'){const px=Math.round(a.length*8.2);const issues=[a.length<30?'Title may be too short.':'',a.length>60?'Title may truncate in many results.':'',px>580?'Estimated desktop width is above the common display range.':''].filter(Boolean);return `Characters: ${a.length}\nEstimated width: ${px}px\nStatus: ${issues.length?issues.join(' '):'Good working length. Review in context.'}`}
if(tool==='serp'){const [desc,url='https://example.com/page']=b.split('\n');return `TITLE (${a.length}): ${a}\nDESCRIPTION (${desc.length}): ${desc}\nURL: ${url}\n\n${a.length>60?'△ Title may truncate.':'✓ Title length is workable.'}\n${desc.length>160?'△ Description may truncate.':'✓ Description length is workable.'}`}
if(tool==='og'){const [desc,image]=b.split('\n');return `<meta property="og:type" content="website">\n<meta property="og:title" content="${esc(a)}">\n<meta property="og:description" content="${esc(desc)}">\n<meta property="og:image" content="${esc(image||'https://example.com/social-card.jpg')}">\n<meta name="twitter:card" content="summary_large_image">`}
if(tool==='robots'){const map:Record<string,string>={Allow:'User-agent: *\nAllow: /','Block admin':'User-agent: *\nDisallow: /admin/\nDisallow: /checkout/','Block all':'User-agent: *\nDisallow: /','Block AI training crawlers':'User-agent: *\nAllow: /\n\nUser-agent: GPTBot\nDisallow: /\n\nUser-agent: CCBot\nDisallow: /\n\nUser-agent: Google-Extended\nDisallow: /'};return `${map[b]||map.Allow}\n\nSitemap: ${a}`}
if(tool==='sitemap'){const urls=a.split('\n').map(x=>x.trim()).filter(Boolean);return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls.map(u=>`  <url>\n    <loc>${esc(u)}</loc>\n    <changefreq>${b}</changefreq>\n  </url>`).join('\n')}\n</urlset>`}
if(tool==='density'){const text=b.toLocaleLowerCase(),key=a.trim().toLocaleLowerCase();const words=text.match(/[\p{L}\p{N}]+/gu)||[];const count=key?text.split(key).length-1:0;const density=words.length?count*Math.max(1,key.split(/\s+/).length)/words.length*100:0;return `Total words: ${words.length}\nExact phrase occurrences: ${count}\nApproximate density: ${density.toFixed(2)}%\n\nUse this as an editing signal, not a ranking target. Cover the topic naturally and satisfy the reader’s intent.`}
if(tool==='slug')return slugify(a,true);
if(tool==='multilingual-slug'){if(b==='Preserve Unicode')return slugify(a,true);if(b==='ASCII only')return slugify(a.normalize('NFKD').replace(/[\u0300-\u036f]/g,''),false);return slugify(Array.from(a).map(ch=>banglaMap[ch]??ch).join(''),false)}
if(tool==='redirect-map'){const pairs=a.split('\n').map(l=>l.split(',').map(x=>x.trim())).filter(p=>p[0]&&p[1]);if(b==='Apache .htaccess')return pairs.map(([x,y])=>`Redirect 301 ${x} ${y}`).join('\n');if(b==='Nginx')return pairs.map(([x,y])=>`rewrite ^${x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}$ ${y} permanent;`).join('\n');return `old_url,new_url,status\n${pairs.map(([x,y])=>`${x},${y},301`).join('\n')}`}
if(tool==='domain'){const core=slugify(a,false).replace(/-/g,''),benefit=slugify(b,false).split('-')[0];const starts=['get','try','use','my','go'];const names=Array.from(new Set([core,`${core}${benefit}`,`${benefit}${core}`,...starts.map(x=>`${x}${core}`)])).filter(Boolean);return names.flatMap(name=>['.com','.io','.co'].map(tld=>`${name}${tld}`)).slice(0,15).join('\n')+'\n\nAvailability is not guaranteed. Verify trademark, domain registration and matching social usernames.'}
if(tool==='product-schema'){const [line,...desc]=b.split('\n');const [price,currency='USD',availability='InStock']=line.split(',').map(x=>x.trim());return `<script type="application/ld+json">\n${JSON.stringify({'@context':'https://schema.org','@type':'Product',name:a,description:desc.join('\n'),offers:{'@type':'Offer',price,priceCurrency:currency,availability:`https://schema.org/${availability}`}},null,2)}\n</script>`}
if(tool==='vitals'){const checks=['LCP: optimize the hero asset, server response and critical CSS.','INP: reduce long JavaScript tasks and unnecessary hydration.','CLS: reserve width and height for images, ads and embeds.','Test representative mobile pages with field data when available.','Measure templates separately: home, category, product, article and checkout.'];return `${a} — ${b}\n\n${checks.map((x,i)=>`[ ] ${i+1}. ${x}`).join('\n')}`}
if(tool==='launch'){const items=['HTTPS and preferred domain redirect','Unique titles and descriptions','Canonical URLs and indexability','Robots.txt and sitemap.xml','404 page and redirect map','Analytics consent and key events','Open Graph image and share preview','Forms, email delivery and validation','Mobile navigation and keyboard access','Performance on representative mobile devices','Backups, monitoring and rollback owner','Privacy, terms and contact details'];return `${a} — ${b}\nGenerated: ${new Date().toISOString().slice(0,10)}\n\n${items.map(x=>`[ ] ${x}`).join('\n')}`}
return ''}
